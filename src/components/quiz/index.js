// import React, { useEffect } from 'react'
// import cs from 'classnames'
// import PropTypes from 'prop-types'
import quizData from '../../containers/App/quizData'

export default function Quiz (props) {
  return (
    <div>{JSON.stringify(quizData)}</div>
  )
}
